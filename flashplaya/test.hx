import flash.events.SampleDataEvent;import flash.media.Sound;import flash.external.ExternalInterface;//import flash.utils.getTimer;class Test {    static function sineWaveGenerator(event:SampleDataEvent) {      if(ExternalInterface.available) {        try {          var dings = ExternalInterface.call("genSound", 4096, event.position);          trace(dings);          var len:Int = Math.floor(dings.length / 2);          trace(len);          for(i in 0...len) {            var val:Float = (dings.charCodeAt(i*2) + (dings.charCodeAt(i*2+1) << 8)) / 32678;            event.data.writeFloat(val);            event.data.writeFloat(val);          }        } catch (msg:Dynamic) {          trace(msg);        }      }    }    static function main() {      trace("Started");      flash.system.Security.allowDomain("localhost");      trace(flash.system.Security.sandboxType);      var mySound:Sound = new Sound();      mySound.addEventListener(SampleDataEvent.SAMPLE_DATA,sineWaveGenerator);      mySound.play();    }}